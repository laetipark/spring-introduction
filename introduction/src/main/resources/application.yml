spring:
  application:
    name: introduction
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/introduction?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: ehdgns1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true